// Imports
import decreaseCounter from '../handlers/decreaseCountHandler.js';
// DOM elements
const decrease = document.getElementById('decrease');
decrease.addEventListener('click', decreaseCounter);
